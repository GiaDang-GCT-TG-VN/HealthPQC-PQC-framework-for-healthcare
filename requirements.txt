# PQC
# liboqs-python installed from source — see docs/liboqs_install.md

# Classical crypto (for hybrid key exchange)
cryptography>=42.0.0

# Web server (Phase 4)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.0.0

# Load testing (Phase 4)
locust>=2.20.0

# Data / utilities
psutil>=5.9.0
pyyaml>=6.0.0
pandas>=2.0.0
matplotlib>=3.7.0

# Certificate scanning (Phase 3)
# cryptography already covers x509 parsing

# HTTP client for testing
httpx>=0.27.0
requests>=2.31.0
