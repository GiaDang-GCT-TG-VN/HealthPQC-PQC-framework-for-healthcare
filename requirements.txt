# requirements.txt — HealthPQC v2.0
# M7 FIX: all versions pinned for reproducible benchmark results
#
# Platform: Python 3.13, ARM64 (Apple Silicon M-series)
# liboqs C library must be installed separately — see docs/liboqs_install.md
#
# Install: pip install -r requirements.txt

# PQC core
liboqs-python==0.14.0

# Classical crypto (for X25519, HKDF in hybrid TLS)
cryptography==43.0.3

# API + load testing
fastapi==0.115.0
uvicorn[standard]==0.32.0
locust==2.32.0

# Config + data
pyyaml==6.0.2
requests==2.32.3

# Testing
pytest==8.3.3
pytest-cov==5.0.0

# Benchmarking utilities
numpy==2.1.2
